# Core Hugging Face stack for QLoRA SFT fine-tuning
# Tested against Kaggle's T4 (16GB) runtime, CUDA 12.x.
transformers==5.16.1
datasets>=3.0.0
peft>=0.13.0
trl>=1.12.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
torch>=2.4.0

# Eval / data handling
scikit-learn>=1.5.0     # stratified splitting

# Experiment tracking (optional but recommended)
wandb>=0.18.0

# Utilities
pandas>=2.2.0
tqdm>=4.66.0
pyyaml>=6.0.0
